# Vision package dependencies voor ROS1 (OAK-D + YOLOv5)

# Deep Learning framework
torch>=2.0.0
torchvision>=0.15.0

# Computer Vision
opencv-python>=4.8.0

# Config parsing
pyyaml>=6.0

# Camera SDK — instaleer apart met --only-binary (geen source compile)
# pip3 install --only-binary :all: depthai
